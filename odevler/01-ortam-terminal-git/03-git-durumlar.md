
- git status -- sildiğim odev.txt deleted olarak görünüyor.

- ctrl + c  --komutu iptal etmek yada kapatmak
- q (quit) çıkmak için 
- git pull origin main --allow-unrelated-histories --pcde olmayan ama gitte olan farklı dosyayı yerele indrmek için

- Updates were rejected because the remote contains work that you do not
hint: have locally. read me dosyasının yerelde bulunmaması sebebiyle bu hatayı aldık.
- git pull origin main --allow-unrelated-histories  ---bu komut ile yerele alabildik

- git init ---sadece ilk kez github a yüklerken atılması gereken komut.

- normalde günlük olarak kullanılması gereken komutlar: 
git add . (Değişiklikleri hazırla)
git commit -m "Yaptığınız değişikliğin açıklaması" (Değişiklikleri paketle)
git push (GitHub'a gönder)