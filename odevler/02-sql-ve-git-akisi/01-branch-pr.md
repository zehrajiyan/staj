git branch ---branchleri getirir.
git switch -c sql-sorguları
git checkout -b sql-sorguları ----aynı işlemi yaparlar. ikisi de branch açar.

git checkout temp-branch -----branch değiştirir.

git push -u origin sql-sorguları ----branchi pushlar.
